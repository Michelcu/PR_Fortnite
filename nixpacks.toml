[phases.setup]
nixPkgs = ["nodejs_20", "chromium"]

[phases.install]
cmds = ["cd backend && npm ci --production=false"]

[phases.build]
cmds = ["echo 'Chromium path:' && which chromium || echo 'Chromium not found in PATH'"]

[start]
cmd = "cd backend && node server.js"
